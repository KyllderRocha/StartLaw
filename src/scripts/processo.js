let table ;

$(document).ready( function () {
    table = $('#TableProcesso').DataTable({
        // scrollY: '53vh',
        scrollCollapse: false,
        paging: true,
        "searching": false
    });
} );